# Database Visualizers

This repository contains two database visualization projects and hosts the downloadable binaries for Tuple:

- [`database-visualizer`](./database-visualizer) — an Astro-based database visualization experience.
- [`tuple`](./tuple) — the Tuple desktop database workspace built with React, Vite, and Tauri.

Each project has its own setup and development instructions in its directory.
